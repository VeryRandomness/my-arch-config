Update system:
sudo pacman -Syu

Install yay:
sudo pacman -S --needed git basedevel
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
cd ~

Packages to install:
sudo pacman -S waybar fastfetch obsidian swaync syncthing tailscale kitty nautilus wofi hyprpaper hypridle hyprshot btop spotify-launcher
yay -S sublime-text-4 anydesk-bin brave-bin

Fonts:
sudo pacman -S otf-font-awesome

Waybar:
waybar &

Tailscale stuff
sudo systemctl enable --now tailscaled
sudo tailscale up
sudo tailscale up
